interface:
  display_name: "Mailctl Email Access"
  short_description: "Use bounded Gmail metadata through canonical mailctl"
  default_prompt: "Use $mailctl-email-access to route one explicit Gmail profile through the consumer-provided mailctl runtime and perform only bounded read-only metadata access."
