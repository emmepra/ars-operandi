interface:
  display_name: "OpenRouter Ops"
  short_description: "Manage OpenRouter keys safely"
  default_prompt: "Use $openrouter-ops to create a workspace-scoped OpenRouter key, store it in 1Password, and run a command without exposing secrets."
policy:
  allow_implicit_invocation: true
